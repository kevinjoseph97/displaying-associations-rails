<h1><%= @category.name %></h1>

<!-- Add code here to list a category's posts. -->

<h3> <%= pluralize(@category.posts.count, 'Post')%>
<ul>
    <% @category.posts.each do |p| %>
        <lil>M<%= link_to p.title, post_path(p) %></lil>
    <% end %>
</ul>
</h3>